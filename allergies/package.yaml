name: allergies
version: 1.0.0.3

dependencies:
  - base

library:
  exposed-modules: Allergies
  source-dirs: src
  dependencies:
    - containers
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - allergies
      - hspec
